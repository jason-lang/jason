Example of A* implemented in Jason

problem:
    starting by a number,
    find a sequence of add1 and add2
    that transforma the initial number into a "goal" number

for instance:
    initial state: 0
    goal state   : 5
    solution     : [ op(add2,2), op(add2,4), op(add1,5) ]
